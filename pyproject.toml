[project]
name = "lyric-storage"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12.9"
dependencies = [
    "flask>=3.0",
    "gunicorn>=26.0.0",
    "markdown>=3.10.2",
    "mutagen>=1.48.1",
]
